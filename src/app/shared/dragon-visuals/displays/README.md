# Shared workstation display primitives

Active Dragon Genetics workstation implementations live together in
`src/app/features/dragon-genetics/workstations`. This directory contains only renderer primitives
shared by multiple workstation surfaces or by the semantic visual-contract runtime.

`shared/` holds copy resolution, reduced-motion handling, teaching-sequence playback, and the
chromosome-pair primitive used by the active Hatchery.
