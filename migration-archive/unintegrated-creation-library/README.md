# Unintegrated creation-library adapter

This folder preserves the original Firestore creation-library repository. It depends on a school-scoped `FirestorePaths` contract and security rules that are not part of PBL Forge yet, so it is intentionally excluded from the Angular application source tree.

The active in-memory creation library is under `src/app/shared/creation-library`.
